const context = {};

const cards = [
  {
    label: 'test.card',
    fields: [
      {
        label: 'test.field',
        value: 'Test Value',
        width: 6
      }
    ]
  }
];

const fields = [];

module.exports = {
  cards,
  fields,
  context
};
